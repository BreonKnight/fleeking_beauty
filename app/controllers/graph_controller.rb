class GraphController < ApplicationController

  # #Counting Place votes
  # def countPlaceVotes
  # 	countVotes(Place.first.upvotes.all.count, Place.first.downvotes.all.count)
  # end

  # #Counting User Votes
  # def countUserVotes
  # 	countVotes(User.upvotes.all.count, User.downvotes.all.count)
  # end

  # #Counting Places the User voted on
  # def countUserPlaceVotes
  # 	countVotes(User.upvotes.places.count, User.downvotes.places.count)
  # end

  # #Counting Votes for all Places
  # def countPlacesVotes
  # 	countVotes(Upvote.all.count, Downvote.all.count)
  # end

end